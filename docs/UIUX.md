# Wearless Landing Page — UI/UX Design Specification
## 수요조사용 프리미엄 랜딩페이지

---

## 🎯 프로젝트 개요

**Wearless**는 쇼핑몰 셀러들을 위한 버티컬 AI 서비스로, 단순 제품 촬영만으로 기존의 복잡한 촬영 프로세스를 대체할 수 있게 합니다. 이 랜딩페이지는 실제 쇼핑몰 업체의 수요를 검증하기 위한 목적으로, 서비스가 이미 출시된 것처럼 구성하되 최종적으로 이메일 수집을 통해 관심도를 측정합니다.

### 핵심 전략
> 셀러가 페이지에 접속하는 순간부터 "내가 겪던 촬영 문제를 이 서비스가 해결해주겠구나"라는 확신을 점진적으로 쌓아가는 구조

**감정 흐름 설계:**
"이건 뭐지?" → "오, 촬영을 이렇게 바꿀 수 있다고?" → "진짜 이 퀄리티가 나온다고?" → "나도 써보고 싶다" → 이메일 입력

---

## 🌐 핵심 백그라운드 컴포넌트

### Luminous Orb Background (`luminous-orb-background.jsx`)
이 컴포넌트는 **페이지 전체의 배경 레이어**로 사용됩니다. Hero 섹션을 중심으로 고정 배치하되, 스크롤 시에도 은은하게 비치도록 합니다.

```
배치: fixed, z-index: 0
위치: 화면 중앙 (Hero 섹션 기준)
크기: 반지름 ~300px 기준 (기존 컴포넌트 그대로)
마우스 반응: parallax 효과 유지 (마우스 이동에 따라 30px 오프셋)
색상 레이어:
  - Layer 1: conic-gradient(#12ADE6, #4C63FC, #12ADE6) — 회전 10.8s
  - Layer 2: conic-gradient(#FF0080, #EE00FF, #00A6FF, #4797FF, #FF8000, #FF00CC) — 역회전 16.2s
  - Layer 3: conic-gradient(#DC4CFC, #12B4E6, #FFFFFF, #DC4CFC) — 회전 13.5s
  - Center Highlight: 백색 radial-gradient, blur 40px
스크롤 연동: Hero에서 가장 선명, 아래로 스크롤 시 opacity 0.3까지 서서히 감소
```

### 모바일 대응
```
모바일(< 768px):
  - Orb 크기 50% 축소 (layer1: 150px, layer2: 125px 등)
  - 마우스 parallax 비활성화 (터치 기기)
  - opacity 전체적으로 0.6으로 줄여 콘텐츠 가독성 확보
```

---

## 🎨 디자인 시스템

### Color Palette

```
── 모노톤 베이스 ──
Primary Background:   #FAFAFA (Off-White)
Secondary Background: #F5F5F7 (Apple Gray — Orb 배경과 통일)
Card Background:      #FFFFFF
Dark Surface:         #1A1A1A (다크 섹션용)
Darker Surface:       #111111 (푸터용)

── 텍스트 ──
Text Primary:    #1A1A1A (거의 블랙)
Text Secondary:  #6B6B6B (미디엄 그레이)
Text Muted:      #9E9E9E (라이트 그레이)
Text Inverse:    #FFFFFF (다크 배경용)

── 포인트 컬러 (Orb에서 추출 — 그라데이션 테두리 전용) ──
Accent Blue:     #12ADE6
Accent Purple:   #4C63FC
Accent Magenta:  #DC4CFC
Accent Pink:     #FF0080
Accent Cyan:     #12B4E6
→ 이 컬러들은 오직 "멀티컬러 그라데이션 테두리 컨테이너"에만 사용.
→ 버튼, 뱃지, 아이콘, 텍스트 등 UI 요소에는 사용하지 않음.

── 멀티컬러 그라데이션 (테두리 컨테이너 전용) ──
Gradient Border: conic-gradient(from 180deg, #12ADE6, #4C63FC, #DC4CFC, #FF0080, #12B4E6, #12ADE6)

── 모노톤 CTA & UI 컬러 ──
CTA Primary:     #1A1A1A (딥 블랙 — 메인 버튼)
CTA Hover:       #333333
CTA Gradient:    linear-gradient(135deg, #1A1A1A 0%, #333333 100%) (미세한 모노 그라데이션)
Badge BG:        #F0F0F0 (라이트 그레이 배경)
Badge Text:      #1A1A1A
Icon Default:    #1A1A1A
Icon Muted:      #9E9E9E

── 유틸리티 ──
Border Default:  #E5E5E5
Border Subtle:   #F0F0F0
Success:         #00C48C
Error:           #FF4757
```

### Typography

```
Font Family: "Pretendard", "Inter", -apple-system, BlinkMacSystemFont, sans-serif

── Desktop ──
Hero Display:    56-64px, 800 weight, -0.03em tracking, 1.1 line-height
Section Title:   40-48px, 700 weight, -0.02em tracking, 1.2 line-height
Heading 2:       28-32px, 600 weight, -0.01em tracking
Heading 3:       20-24px, 600 weight
Body Large:      18px, 400 weight, 1.7 line-height
Body:            16px, 400 weight, 1.6 line-height
Caption:         14px, 400 weight, 1.5 line-height
Small:           12px, 500 weight, 1.4 line-height
Button:          16px, 600 weight, 0.02em tracking

── Mobile (< 768px) ──
Hero Display:    36-40px, 800 weight
Section Title:   28-32px, 700 weight
Heading 2:       22-24px, 600 weight
Body:            15px, 400 weight
```

### Spacing & Layout

```
Container Max Width: 1200px (padding 24px 양쪽)
Section Vertical Padding:
  Desktop: 100-140px
  Tablet:  80-100px
  Mobile:  60-80px

Card Padding: 32-40px (Desktop), 20-24px (Mobile)

Border Radius:
  Small:  8px
  Medium: 12px
  Large:  16px
  XL:     24px
  XXL:    32px (대형 컨테이너)
  Pill:   9999px
```

### Shadow System

```
Subtle:    0 1px 3px rgba(0, 0, 0, 0.04)
Card:      0 4px 24px rgba(0, 0, 0, 0.06)
Elevated:  0 8px 40px rgba(0, 0, 0, 0.08)
Floating:  0 20px 60px rgba(0, 0, 0, 0.10)
Glow:      0 0 40px rgba(0, 0, 0, 0.08)
```

---

## ✨ 멀티컬러 그라데이션 테두리 컨테이너

크기가 큰 주요 컴포넌트(Feature 카드, 비디오 컨테이너, 요금제 카드, 후기 카드 등)에 적용되는 **시그니처 테두리 스타일**입니다.

### 구현 방식
```
구조: 바깥 wrapper div + 안쪽 content div

Wrapper:
  - padding: 2px (테두리 두께)
  - border-radius: 24px (둥근 사각형)
  - background: conic-gradient(from 180deg, #12ADE6, #4C63FC, #DC4CFC, #FF0080, #EE00FF, #12B4E6, #12ADE6)
  - animation: gradientSpin 8s linear infinite (천천히 회전하는 그라데이션)

Inner Content:
  - background: #FFFFFF (또는 해당 섹션 배경색)
  - border-radius: 22px (wrapper보다 2px 작게)
  - 내부 콘텐츠 배치

Hover 상태:
  - padding: 2px → 3px (테두리 살짝 두꺼워짐)
  - box-shadow: 0 0 30px rgba(76, 99, 252, 0.2) 추가
  - transition: 300ms ease

@keyframes gradientSpin:
  from { filter: hue-rotate(0deg); }
  to { filter: hue-rotate(360deg); }
  → 또는 pseudo-element를 회전시키는 방식
```

### 적용 대상
```
✅ 적용: 데모 영상 컨테이너, 핵심 기능 카드, 서비스 영상 컨테이너, 후기 카드, 요금제 카드(추천), 문의 폼 컨테이너
❌ 미적용: 네비게이션 바, FAQ 아코디언, 푸터, 작은 보조 UI 요소
```

---

## ✨ 글로벌 애니메이션 시스템

### Scroll Reveal (IntersectionObserver 기반)
```
모든 섹션의 주요 콘텐츠에 적용:
  - 초기 상태: opacity: 0, translateY(40px)
  - 트리거: viewport 진입 시 (threshold: 0.15)
  - 실행: opacity: 1, translateY(0)
  - 속도: 600ms, easing: cubic-bezier(0.16, 1, 0.3, 1)
  - 순차 딜레이: 자식 요소마다 100ms씩 stagger
```

### Hover Micro-interactions
```
카드: translateY(-6px) + shadow 증가, 300ms ease
버튼: translateY(-2px) + glow 강화, 250ms ease
이미지: scale(1.03), 400ms ease
링크: color transition + underline slide-in, 200ms
```

### Continuous Animations
```
Orb: 계속 회전 (배경)
그라데이션 테두리: 색상 천천히 시프트 (8s cycle)
Trust 지표 숫자: 카운트업 애니메이션 (viewport 진입 시)
```

### Timing Standards
```
Micro: 150-200ms (hover, focus)
Short: 250-350ms (토글, 탭 전환)
Medium: 400-600ms (스크롤 reveal, 모달)
Long: 800-1200ms (히어로 입장, 카운트업)
Easing: cubic-bezier(0.16, 1, 0.3, 1) — 기본
         cubic-bezier(0.4, 0, 0.2, 1) — 빠른 응답
```

---

## 📱 반응형 브레이크포인트

```
Desktop Large: 1440px+ (최대 너비 제한, 중앙 정렬)
Desktop:       1024px - 1439px (기본 레이아웃)
Tablet:        768px - 1023px (2열 → 1열, 여백 축소)
Mobile:        < 768px (완전 단일 열, 터치 최적화)

모바일 특별 규칙:
  - 모든 버튼 최소 높이: 48px (터치 타겟)
  - 좌우 패딩: 20px
  - 폰트 사이즈 전체적으로 85-90% 스케일
  - 수평 스크롤 절대 금지
  - 이미지 aspect-ratio 유지하되 width: 100%
```

---

## 🧭 Section 1: Navigation Bar

### 레이아웃
```
위치: fixed top, z-index: 50
높이: 72px (Desktop), 60px (Mobile)
배경: rgba(250, 250, 250, 0.85) + backdrop-filter: blur(20px) + saturate(1.8)
하단 보더: 1px solid rgba(0, 0, 0, 0.06)
전환: 스크롤 시 배경 opacity 0.85 → 0.95, shadow 추가
내부: max-width 1200px, 가운데 정렬, 양쪽 padding 24px
```

### 구성 (3단 배치)
```
왼쪽 — 브랜드:
  - Wearless 로고 (아이콘 + 워드마크)
  - 로고 아이콘: 작은 심볼 (추상적 카메라/옷 형태, #1A1A1A)
  - 워드마크: "Wearless" — 20px, 700 weight, #1A1A1A
  - 클릭 시 최상단 스크롤

가운데 — 내비게이션 링크:
  - "홈" | "주요 기능" | "요금제"
  - 16px, 500 weight, #6B6B6B
  - Hover: #1A1A1A, 밑줄 slide-in 애니메이션 (#1A1A1A)
  - Active(현재 섹션): #1A1A1A, 밑줄 상시 표시
  - 클릭 시 해당 섹션으로 smooth scroll
  - 각 링크 간격: 40px

오른쪽 — CTA 버튼:
  - "시작하기" — Primary Button
    Background: #1A1A1A
    Text: #FFFFFF, 15px, 600 weight
    Padding: 10px 24px
    Border-radius: 9999px (pill)
    Hover: #333333, translateY(-1px)
  - "문의하기" — Ghost Button
    Background: transparent
    Border: 1.5px solid #E5E5E5
    Text: #6B6B6B, 15px, 500 weight
    Padding: 10px 24px
    Border-radius: 9999px
    Hover: border-color #1A1A1A, text #1A1A1A
    왼쪽 마진: 12px
```

### 모바일 (< 768px)
```
왼쪽: 로고만 (워드마크 유지)
가운데: 숨김
오른쪽: 햄버거 아이콘 (24px, #1A1A1A)

햄버거 메뉴 열림:
  - 전체 화면 오버레이, 배경 #FAFAFA
  - 세로 중앙 정렬된 링크 목록
  - 각 링크: 24px, 600 weight, 간격 32px
  - 하단에 "시작하기" + "문의하기" 버튼 (full-width)
  - 닫기: X 아이콘 (우상단)
  - 진입 애니메이션: fade + slide from right (300ms)
```

---

## 🎬 Section 2: Hero + Demo Video

### Hero Area (전체 Viewport)
```
높이: 100vh (최소 700px)
배경: Luminous Orb Background 컴포넌트 (z-index: 0)
콘텐츠: z-index: 10, flex column, 수직·수평 중앙 정렬
패딩: 네비게이션 높이(72px) 고려 + 상단 여유 40px
```

### Hero 텍스트
```
Badge (제목 위):
  - "AI-Powered Product Photography"
  - 12px, 600 weight, letter-spacing: 0.1em, uppercase
  - 색상: #1A1A1A
  - 배경: #F0F0F0, border-radius: 9999px
  - 패딩: 6px 16px

Main Headline (2-3줄, 중앙 정렬):
  줄 1: "제품 사진 하나로" — 56-64px, 800 weight, #1A1A1A
  줄 2: "쇼핑몰 촬영의 모든 것을" — 동일
  줄 3: "대체하다" — 동일, #1A1A1A (전체 모노톤 통일)
  max-width: 720px

  입장 애니메이션: 각 줄 순차 fade-up (200ms stagger)

Sub-headline:
  "스튜디오, 모델, 조명 없이. 제품 사진만 올리면 완성됩니다."
  — 18px, 400 weight, #6B6B6B
  — max-width: 520px, 중앙 정렬
  — margin-top: 24px
  — 입장 애니메이션: headline 후 300ms 딜레이

CTA 영역 (Sub-headline 아래 40px):
  Primary: "지금 시작하기"
    — 배경: #1A1A1A
    — #FFFFFF, 17px, 600 weight
    — padding: 16px 40px, border-radius: 9999px
    — shadow: 0 4px 24px rgba(0, 0, 0, 0.15)
    — hover: #333333, translateY(-3px), shadow 강화
  Secondary: "서비스 소개 보기 ↓"
    — transparent bg, #6B6B6B text, 15px
    — hover: #1A1A1A
    — 아래 화살표 bounce 애니메이션 (subtle, 2s cycle)
  두 버튼 가로 배치, 간격 16px

Trust Indicator (CTA 아래 32px):
  "이미 1,200+ 셀러가 관심을 보이고 있습니다"
  — 14px, #9E9E9E
  — 작은 아바타 스택 (3-4개 원형 프로필 겹침) + 텍스트 가로 배치
```

### Demo Video 영역 (Hero 텍스트 아래, Viewport 내)
```
위치: Hero 텍스트 블록 아래 60px, 여전히 100vh viewport 안에 배치
    → 텍스트가 상단 40%쯤, 비디오가 하단 50%쯤 차지하도록 비율 조정
    → 뷰포트 안에 모두 보여야 함

컨테이너:
  - 멀티컬러 그라데이션 테두리 컨테이너 적용 ✅
  - max-width: 900px, 중앙 정렬
  - aspect-ratio: 16/9
  - border-radius: 24px (wrapper), 22px (inner)
  - overflow: hidden
  - shadow: Floating

내부:
  - 비디오 썸네일 이미지 (전체 커버)
  - 중앙 재생 버튼:
    원형, 64px, rgba(255,255,255,0.95)
    ▶ 아이콘: #1A1A1A, 24px
    backdrop-filter: blur(10px)
    hover: scale(1.1), shadow 추가
  - 클릭 시: 썸네일 → 실제 영상 재생 (임베드 또는 모달)
  - 하단 오버레이: 가벼운 그라데이션 (투명 → 약간 어두움), 영상 길이 표시

입장 애니메이션: fade-up + scale(0.95 → 1.0), 800ms, CTA 후 400ms 딜레이

모바일:
  - max-width: 100% (좌우 마진 20px)
  - aspect-ratio 유지
  - 재생 버튼 48px로 축소
```

---

## 🔑 Section 3: 핵심 기능 소개 (4가지)

### 섹션 헤더
```
배경: #FFFFFF
상단 패딩: 120px

Section Label:
  "CORE FEATURES"
  — 13px, 600 weight, letter-spacing: 0.12em, #9E9E9E

Section Title:
  "왜 Wearless인가요?"
  — Section Title 사이즈 (40-48px), 700 weight, #1A1A1A
  — margin-top: 16px

Section Subtitle:
  "기존 촬영 프로세스의 한계를 넘어, 제품 사진 하나로 모든 것을 해결합니다."
  — 18px, #6B6B6B, max-width: 560px
  — margin-top: 16px

모두 중앙 정렬
```

### 기능 카드 레이아웃 (4개, 교차 배치)
```
구조: 각 기능은 이미지 + 텍스트가 좌우 교차로 배치됩니다.
  - Feature 1: 이미지 왼쪽, 텍스트 오른쪽
  - Feature 2: 텍스트 왼쪽, 이미지 오른쪽
  - Feature 3: 이미지 왼쪽, 텍스트 오른쪽
  - Feature 4: 텍스트 왼쪽, 이미지 오른쪽

각 Feature 행:
  - max-width: 1100px, 중앙 정렬
  - 두 열: 50% / 50% (gap: 60px)
  - 수직 정렬: center
  - 행 간 간격: 100px (Desktop), 60px (Mobile)
```

### 각 Feature 구성

```
이미지 영역:
  - 멀티컬러 그라데이션 테두리 컨테이너 적용 ✅
  - 내부: 목업 이미지 또는 스크린샷 (서비스 UI 시연 컷)
  - border-radius: 20px
  - aspect-ratio: 4/3 또는 16/10
  - overflow: hidden
  - hover: 내부 이미지 scale(1.03), 400ms ease
  - 입장 애니메이션: 이미지 방향에서 slide-in (왼쪽이면 왼쪽에서)

텍스트 영역:
  Feature Number:
    - "01" ~ "04"
    - 14px, 700 weight, #9E9E9E
  Title:
    - 28-32px, 700 weight, #1A1A1A
    - margin-top: 12px
  Description:
    - 16-18px, 400 weight, #6B6B6B, line-height: 1.7
    - margin-top: 16px, max-width: 440px
  Highlight Tag (선택):
    - 작은 pill 뱃지, #F0F0F0 배경, #1A1A1A 텍스트
    - margin-top: 20px
  입장 애니메이션: fade-up, 이미지보다 200ms 딜레이
```

### 기능 4가지 내용 (예시 카피)

```
Feature 01: "원클릭 배경 제거 & 생성"
  설명: "제품 사진을 올리면 배경이 자동으로 분리되고,
        원하는 분위기의 새로운 배경이 즉시 생성됩니다."
  태그: "배경 자동 생성"

Feature 02: "모델 없는 착용 컷"
  설명: "모델 섭외, 스튜디오 예약 없이도 자연스러운
        착용 이미지를 만들어 드립니다."
  태그: "AI 가상 피팅"

Feature 03: "일관된 쇼핑몰 톤 유지"
  설명: "한번 설정한 브랜드 톤 앤 매너에 맞춰
        모든 제품 이미지가 통일된 퀄리티로 생성됩니다."
  태그: "브랜드 일관성"

Feature 04: "대량 생산, 즉시 적용"
  설명: "수백 장의 제품 이미지도 몇 분 안에 완성.
        바로 쇼핑몰에 업로드할 수 있는 고해상도 결과물."
  태그: "대량 처리"
```

### 모바일 (< 768px)
```
- 모든 Feature가 단일 열 (이미지 상단, 텍스트 하단)
- 교차 배치 해제, 일관되게 이미지 → 텍스트 순서
- 이미지 width: 100%, aspect-ratio 유지
- 텍스트 좌측 정렬 (중앙 아님)
- 행 간 간격: 48px
```

---

## 🔄 Section 4: 서비스 이용 과정 (4 Steps)

### 섹션 헤더
```
배경: #F5F5F7 (약간 어두운 배경으로 전환 — 시각적 분리)

Section Label: "HOW IT WORKS"
Section Title: "4단계로 끝나는 촬영"
Section Subtitle: "복잡한 설정 없이, 업로드부터 결과물까지 단 몇 분."
모두 중앙 정렬
```

### 스텝 카드 레이아웃
```
구조: 4개 카드 가로 배치 (1행 4열)
  - max-width: 1100px
  - gap: 24px
  - 각 카드: flex: 1

카드 사이 연결선:
  - 카드 사이에 가느다란 화살표/점선 (→)
  - 색상: #E5E5E5, 스텝 완료 느낌

입장 애니메이션: 왼쪽부터 순차 fade-up (각 150ms stagger)
```

### 각 스텝 카드
```
컨테이너:
  - background: #FFFFFF
  - border-radius: 20px
  - padding: 32px 28px
  - shadow: Card
  - hover: translateY(-6px) + shadow Elevated, 300ms
  - 그라데이션 테두리 미적용 (깔끔하게)

Step Number:
  - 원형 배지, 40px
  - 배경: #1A1A1A
  - 텍스트: #FFFFFF, 16px, 700 weight
  - "1", "2", "3", "4"

Step Title:
  - 20px, 600 weight, #1A1A1A
  - margin-top: 20px

Step Description:
  - 15px, 400 weight, #6B6B6B, line-height: 1.6
  - margin-top: 12px

Step Icon/Illustration:
  - 각 카드 상단에 간단한 아이콘 또는 미니 일러스트 (48px)
  - 모노톤 기본 (#9E9E9E), hover 시 #1A1A1A 전환
```

### 4단계 내용

```
Step 1: "제품 사진 업로드"
  설명: "스마트폰으로 찍은 사진이면 충분합니다. 바닥컷, 행거컷 모두 OK."
  아이콘: 업로드/카메라

Step 2: "스타일 선택"
  설명: "원하는 배경, 분위기, 촬영 컨셉을 선택하세요."
  아이콘: 팔레트/슬라이더

Step 3: "AI 자동 생성"
  설명: "몇 초 만에 프로페셔널 퀄리티의 이미지가 완성됩니다."
  아이콘: 스파클/매직

Step 4: "다운로드 & 적용"
  설명: "고해상도 결과물을 바로 다운로드해서 쇼핑몰에 적용하세요."
  아이콘: 다운로드/체크
```

### 모바일 (< 768px)
```
- 4열 → 2열 × 2행 (Tablet), 1열 × 4행 (Mobile)
- 카드 사이 화살표 → 세로 점선으로 전환
- 카드 패딩 축소 (24px)
```

---

## 🎥 Section 5: 서비스 이용 영상

### 섹션 헤더
```
배경: #FFFFFF

Section Label: "SEE IT IN ACTION"
Section Title: "직접 확인하세요"
Section Subtitle: "실제 서비스 이용 과정을 영상으로 확인해 보세요."
중앙 정렬
```

### 영상 컨테이너
```
- 멀티컬러 그라데이션 테두리 컨테이너 적용 ✅
- max-width: 960px, 중앙 정렬
- aspect-ratio: 16/9
- border-radius: 24px (wrapper), 22px (inner)
- shadow: Floating
- 내부: 영상 썸네일 + 중앙 재생 버튼 (Hero Demo와 동일 스타일)

입장 애니메이션: fade-up + scale(0.96 → 1.0), 600ms

모바일:
  - width: 100% (좌우 패딩 20px)
  - border-radius: 16px
```

---

## 💬 Section 6: 사용자 후기

### 섹션 헤더
```
배경: #F5F5F7

Section Label: "TESTIMONIALS"
Section Title: "셀러들의 실제 반응"
Section Subtitle: "Wearless를 먼저 경험한 셀러들의 이야기"
중앙 정렬
```

### 후기 카드 레이아웃
```
구조: 3개 카드 가로 배치 (Desktop), 가로 스크롤 가능 (Mobile)
max-width: 1100px, gap: 28px
```

### 각 후기 카드
```
- 멀티컬러 그라데이션 테두리 컨테이너 적용 ✅
- border-radius: 20px (wrapper), 18px (inner)
- inner background: #FFFFFF
- padding: 32px

상단:
  ★★★★★ (별점 5개)
  — 색상: #FFB800 (골드), 18px

인용문:
  "촬영 비용이 월 200만원에서 거의 0원이 됐어요.
   이걸 왜 이제야 알았는지..."
  — 18px, 500 weight, #1A1A1A, line-height: 1.7
  — 이탤릭 또는 큰따옴표 스타일

프로필:
  - 원형 아바타 (48px) + 이름/직함 가로 배치
  - 이름: 16px, 600 weight, #1A1A1A ("김○○ 대표")
  - 직함: 14px, 400 weight, #9E9E9E ("여성의류 전문 쇼핑몰")

hover: 전체 카드 translateY(-4px), shadow 증가
입장 애니메이션: 순차 fade-up, 150ms stagger
```

### 후기 내용 (예시 3개)

```
후기 1:
  "촬영 비용이 월 200만원에서 거의 0원이 됐어요. 퀄리티는 오히려 더 일관성 있고요."
  — 김○○ 대표 / 여성의류 전문 쇼핑몰

후기 2:
  "신상품 등록 속도가 3배 빨라졌어요. 이제 촬영 일정에 맞출 필요가 없으니까요."
  — 이○○ MD / 온라인 편집샵

후기 3:
  "모델 섭외, 스튜디오 예약... 이 스트레스가 사라진 것만으로도 가치 있습니다."
  — 박○○ 실장 / 남성복 브랜드
```

### 모바일 (< 768px)
```
- 3열 → 가로 스크롤 캐러셀
- snap scroll 적용 (scroll-snap-type: x mandatory)
- 각 카드 width: 85vw
- 하단 인디케이터 도트 (현재 위치 표시)
- 좌우 스와이프 가능
```

---

## 💰 Section 7: 요금제

### 섹션 헤더
```
배경: #FFFFFF

Section Label: "PRICING"
Section Title: "합리적인 요금제"
Section Subtitle: "비즈니스 규모에 맞는 플랜을 선택하세요."
중앙 정렬
```

### 요금제 카드 레이아웃
```
구조: 3개 카드 가로 배치
max-width: 1100px, gap: 24px
가운데 카드(추천)만 약간 크게 + 강조 처리
```

### 기본 플랜 카드
```
컨테이너:
  - background: #FFFFFF
  - border: 1.5px solid #E5E5E5
  - border-radius: 20px
  - padding: 40px 32px
  - shadow: Card

플랜명: "Starter"
  — 16px, 600 weight, #6B6B6B

가격: "₩29,000/월"
  — 가격 숫자: 40px, 800 weight, #1A1A1A
  — "/월": 16px, 400 weight, #9E9E9E

구분선: 1px solid #F0F0F0, margin 24px 0

기능 리스트 (체크마크 + 텍스트):
  ✓ 월 50장 생성
  ✓ 기본 배경 템플릿
  ✓ 표준 해상도
  ✓ 이메일 지원
  — 각 항목: 15px, #6B6B6B, 간격 14px
  — ✓ 아이콘: #1A1A1A

CTA 버튼:
  "시작하기"
  — Ghost style (border #E5E5E5, text #1A1A1A)
  — full-width, border-radius: 12px, padding: 14px
  — hover: border #1A1A1A, background #1A1A1A, text #FFFFFF
```

### 추천 플랜 카드 (가운데, 강조)
```
컨테이너:
  - 멀티컬러 그라데이션 테두리 컨테이너 적용 ✅
  - inner background: #FFFFFF
  - scale(1.05) 기본 (다른 카드보다 살짝 큼)
  - shadow: Elevated

"추천" 뱃지 (카드 상단 바깥):
  - pill 형태, #1A1A1A 배경, #FFFFFF 텍스트
  - "MOST POPULAR", 12px, 600 weight
  - 카드 상단 중앙에 걸쳐진 형태 (negative margin)

플랜명: "Professional"
  — 16px, 600 weight, #1A1A1A

가격: "₩79,000/월"
  — 동일 스타일

기능 리스트:
  ✓ 월 300장 생성
  ✓ 프리미엄 배경 & 모델 컷
  ✓ 고해상도 출력
  ✓ 브랜드 톤 설정
  ✓ 우선 지원
  — ✓ 아이콘: #1A1A1A

CTA 버튼:
  "시작하기"
  — 배경: #1A1A1A
  — #FFFFFF 텍스트
  — full-width, border-radius: 12px, padding: 14px
  — shadow: 0 4px 16px rgba(0, 0, 0, 0.15)
  — hover: #333333, translateY(-2px)
```

### 엔터프라이즈 플랜 카드
```
기본 플랜과 동일 스타일, 내용만 다름:

플랜명: "Enterprise"
가격: "맞춤 견적"
  — 40px 자리에 "문의" 또는 "Custom"

기능 리스트:
  ✓ 무제한 생성
  ✓ 전용 API 연동
  ✓ 커스텀 모델 학습
  ✓ 전담 매니저
  ✓ SLA 보장

CTA 버튼:
  "문의하기"
  — Ghost style
```

### 모바일 (< 768px)
```
- 3열 → 가로 스크롤 캐러셀 (추천 플랜 먼저 보이도록)
- 또는 1열 세로 스택 (추천 플랜이 맨 위)
- scale(1.05) 해제 (동일 크기)
- 추천 뱃지 유지
```

---

## 📩 Section 8: 문의란 (+ 이메일 수집 — 핵심 CTA)

### 섹션 구성
```
배경: #1A1A1A (다크 섹션 — 강한 대비로 시선 집중)
텍스트: #FFFFFF 기본
패딩: 120px 상하
```

### 레이아웃
```
max-width: 800px, 중앙 정렬

컨테이너:
  - 멀티컬러 그라데이션 테두리 컨테이너 적용 ✅
  - inner background: #222222
  - border-radius: 28px (wrapper), 26px (inner)
  - padding: 56px 48px (Desktop), 32px 24px (Mobile)
```

### 내용
```
Section Title:
  "지금 바로 시작하세요"
  — 40px, 700 weight, #FFFFFF
  — 중앙 정렬

Section Subtitle:
  "이메일을 남겨주시면, 서비스 오픈 시 가장 먼저 알려드립니다.
   얼리버드 혜택도 함께 받아보세요."
  — 18px, rgba(255,255,255,0.65)
  — max-width: 480px, 중앙 정렬, margin-top: 16px

이메일 입력 폼 (margin-top: 40px):
  구조: 이메일 input + 제출 버튼 가로 배치

  Input:
    - 배경: rgba(255,255,255,0.08)
    - 테두리: 1.5px solid rgba(255,255,255,0.15)
    - 텍스트: #FFFFFF, 16px
    - placeholder: "이메일을 입력하세요" (rgba(255,255,255,0.35))
    - padding: 16px 20px
    - border-radius: 12px (왼쪽만) 또는 전체 12px
    - focus: border-color #FFFFFF, subtle glow 효과
    - width: 65%

  Submit Button:
    - "알림 신청"
    - 배경: #FFFFFF
    - #1A1A1A 텍스트, 16px, 600 weight
    - padding: 16px 32px
    - border-radius: 12px (오른쪽만) 또는 전체 12px
    - hover: rgba(255,255,255,0.85)
    - width: 35%
    - 또는 input과 버튼을 하나의 container로 감싸서 pill 형태로 구성

  제출 후:
    - input + 버튼 → 성공 메시지로 전환
    - "✓ 신청이 완료되었습니다! 오픈 시 가장 먼저 알려드릴게요."
    - 체크마크 애니메이션 + fade-in
    - 색상: #00C48C (Success)

보안 안내 (폼 아래):
  "🔒 스팸 없이, 서비스 소식만 보내드립니다."
  — 13px, rgba(255,255,255,0.35)
  — margin-top: 16px
```

### 모바일 (< 768px)
```
- input과 버튼 세로 스택
- input: width 100%, border-radius: 12px (전체)
- 버튼: width 100%, border-radius: 12px (전체), margin-top: 12px
```

---

## ❓ Section 9: FAQ

### 섹션 헤더
```
배경: #FAFAFA

Section Label: "FAQ"
Section Title: "자주 묻는 질문"
중앙 정렬
```

### 아코디언 레이아웃
```
max-width: 760px, 중앙 정렬
각 항목 사이: 12px gap

아코디언 아이템:
  - background: #FFFFFF
  - border: 1px solid #F0F0F0
  - border-radius: 16px
  - padding: 24px 28px

  질문 (접힌 상태):
    - 18px, 600 weight, #1A1A1A
    - 오른쪽: + 아이콘 (회전 애니메이션)
    - cursor: pointer
    - hover: background #FAFAFA

  답변 (펼친 상태):
    - + → × 로 회전 (45deg rotate, 300ms)
    - 답변 텍스트 slide-down + fade-in (300ms)
    - 16px, 400 weight, #6B6B6B, line-height: 1.7
    - padding-top: 16px
    - 상단에 1px solid #F0F0F0 구분선

  그라데이션 테두리 미적용 (깔끔하게)
```

### FAQ 내용 (예시 6개)

```
Q1: "어떤 종류의 제품 사진을 업로드할 수 있나요?"
A1: "의류, 잡화, 액세서리 등 대부분의 패션 제품을 지원합니다. 바닥컷, 행거컷, 마네킹컷 등 어떤 형태든 괜찮습니다. 스마트폰으로 촬영한 사진도 충분합니다."

Q2: "생성된 이미지의 퀄리티는 어느 정도인가요?"
A2: "전문 스튜디오에서 촬영한 것과 동등한 수준의 고해상도 이미지를 제공합니다. 실제 쇼핑몰에 바로 업로드할 수 있는 퀄리티입니다."

Q3: "한 번에 여러 장을 처리할 수 있나요?"
A3: "네, 대량 업로드 기능을 제공합니다. Professional 플랜 이상에서 배치 처리가 가능하며, 수백 장을 한 번에 처리할 수 있습니다."

Q4: "생성에 걸리는 시간은 얼마나 되나요?"
A4: "1장 기준 약 10-30초 정도 소요됩니다. 배치 처리 시에도 장당 처리 시간은 동일하며, 병렬 처리로 빠르게 완료됩니다."

Q5: "무료 체험이 가능한가요?"
A5: "네, 가입 즉시 무료 크레딧이 제공됩니다. 결제 정보 없이도 서비스를 체험해 보실 수 있습니다."

Q6: "기존 쇼핑몰 플랫폼과 연동되나요?"
A6: "카페24, 스마트스토어, 쿠팡 등 주요 플랫폼과의 연동을 지원하며, API를 통한 커스텀 연동도 가능합니다."
```

### 모바일 (< 768px)
```
- max-width: 100% (좌우 패딩 20px)
- 패딩 축소: 20px 24px
- 질문 글꼴: 16px
```

---

## 🔻 Section 10: Footer

### 레이아웃
```
배경: #111111
텍스트: rgba(255,255,255,0.5) 기본
패딩: 48px 0 32px 0
border-top: 1px solid rgba(255,255,255,0.08)
```

### 구성 (간결하게)
```
max-width: 1200px, 중앙 정렬

상단 행 (좌우 배치):
  왼쪽:
    - Wearless 로고 (흰색 버전)
    - "쇼핑몰 촬영의 새로운 기준" — 14px, rgba(255,255,255,0.4)

  오른쪽:
    - 링크: "이용약관" | "개인정보처리방침" | "문의하기"
    - 14px, rgba(255,255,255,0.5)
    - hover: rgba(255,255,255,0.9)
    - 구분자: · (middle dot)

하단 행 (중앙):
  - "© 2025 Wearless. All rights reserved."
  - 13px, rgba(255,255,255,0.3)
  - margin-top: 24px
  - 상단에 1px solid rgba(255,255,255,0.06) 구분선
```

### 모바일 (< 768px)
```
- 상단 행: 세로 스택 (왼쪽 요소 → 오른쪽 요소)
- 모든 요소 중앙 정렬
- 링크 세로 배치, 간격 16px
```

---

## 🧩 공통 재사용 컴포넌트

### 1. Gradient Border Container (시그니처 컴포넌트)
```jsx
// 큰 카드/컨테이너에 적용하는 멀티컬러 테두리
<div className="gradient-border-wrapper">
  <div className="gradient-border-inner">
    {children}
  </div>
</div>

Wrapper:
  padding: 2px
  border-radius: 24px
  background: conic-gradient(from 180deg, #12ADE6, #4C63FC, #DC4CFC, #FF0080, #EE00FF, #12B4E6, #12ADE6)
  animation: gradientShift 8s linear infinite

Inner:
  background: inherit (해당 섹션 배경색)
  border-radius: 22px
  overflow: hidden
```

### 2. Primary CTA Button
```
Background: #1A1A1A
Text: #FFFFFF, 16px, 600 weight
Padding: 14px 32px
Border-radius: 9999px
Shadow: 0 4px 20px rgba(0, 0, 0, 0.15)
Hover: #333333, translateY(-2px), shadow 증가
Active: scale(0.98)
Transition: all 250ms ease
```

### 3. Ghost Button
```
Background: transparent
Border: 1.5px solid #E5E5E5
Text: #6B6B6B, 16px, 500 weight
Padding: 14px 32px
Border-radius: 9999px
Hover: border-color #1A1A1A, text-color #1A1A1A
Active: scale(0.98)
```

### 4. Section Header Pattern
```
Label: 13px, 600 weight, uppercase, letter-spacing 0.12em, #9E9E9E
Title: Section Title size, 700 weight, #1A1A1A (or #FFF on dark)
Subtitle: 18px, 400 weight, #6B6B6B (or rgba(255,255,255,0.6) on dark)
간격: Label → Title 16px, Title → Subtitle 16px
모두 중앙 정렬
```

### 5. Video Container
```
멀티컬러 그라데이션 테두리 + 내부 영상
재생 버튼: 중앙, 원형, glass-morphism 스타일
Shadow: Floating
```

---

## 📐 전체 페이지 흐름 요약

```
┌─────────────────────────────────┐
│ Navigation Bar (fixed)          │ — 로고, 링크, CTA
├─────────────────────────────────┤
│ Hero + Demo Video (100vh)       │ — Orb 배경, 메인 카피, 영상
├─────────────────────────────────┤
│ Core Features (4개 교차)         │ — 이미지 + 설명, 좌우 교대
├─────────────────────────────────┤
│ How It Works (4 Steps)          │ — 4단계 카드 가로 배치
├─────────────────────────────────┤
│ Service Video                   │ — 이용 영상 대형 컨테이너
├─────────────────────────────────┤
│ Testimonials                    │ — 후기 카드 3개
├─────────────────────────────────┤
│ Pricing                         │ — 3-tier 요금제
├─────────────────────────────────┤
│ Contact / Email CTA (Dark)      │ — 이메일 수집 (핵심 전환)
├─────────────────────────────────┤
│ FAQ                             │ — 아코디언 6개
├─────────────────────────────────┤
│ Footer                          │ — 간결한 정보
└─────────────────────────────────┘
```

---

## 🎯 디자인 원칙 (꼭 지켜야 할 것)

1. **모노톤 UI 철저**: 버튼, 뱃지, 아이콘, 텍스트 등 모든 UI 요소는 블랙/그레이/화이트 모노톤. 컬러풀한 포인트 컬러 사용 금지.
2. **Orb 컬러는 테두리 전용**: 멀티컬러 그라데이션(#12ADE6, #4C63FC, #DC4CFC, #FF0080, #12B4E6)은 오직 그라데이션 테두리 컨테이너에만 적용. 이것이 페이지에서 유일한 컬러 포인트이므로, 오히려 더 임팩트 있게 느껴짐.
3. **반응형 필수**: 모든 섹션은 Desktop(1200px+), Tablet(768px-1023px), Mobile(<768px) 세 단계로 대응. 모바일 가독성이 최우선.
4. **애니메이션 절제**: 모든 섹션에 scroll reveal 기본 적용, hover 인터랙션 포함. 단, 과하지 않게 — 부드럽고 자연스러워야 함.
5. **셀러 관점 카피**: 기술적 용어 최소화. "AI가 생성" 대신 "자동으로 완성", "신경망" 대신 "스마트 시스템" 등.
6. **전환 최적화**: 이메일 입력란은 문의 섹션뿐 아니라 Hero CTA("지금 시작하기")에도 연결. 모든 "시작하기" 버튼은 이메일 섹션으로 smooth scroll.
7. **고급진 여백**: 섹션 간 충분한 breathing room. 빽빽하지 않게.
8. **Luminous Orb는 핵심**: Hero 배경으로 존재감 있되, 아래 콘텐츠 영역에서는 은은하게 비치는 정도로.
